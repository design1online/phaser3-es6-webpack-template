/**
* Custom global values that will be added onto the game object
*/
export default {
  fullScreen: true, // toggle to turn off full screen ressizing
  debugMode: true, // toggle the game's debug mode
  textColor: '#00ff00', // this default text color
  fillColor: '0xff00ff', // default fill color
  assetPath: '../../assets/', // the default asset path
};
